# Paron customer PWA

Install and run this app from a secure origin (HTTPS, or `localhost` during development). The real Bluetooth action requires Chrome on Android and the `merchant-android` companion app. The simulation switch lets the entire payment journey run without BLE hardware.

See [`../docs/two-phone-demo.md`](../docs/two-phone-demo.md) for setup and the demo script.
